// The public site has a single language. Keep the component API for callers.
import type { ComponentProps } from 'react'

export default function LanguageSwitcher(_props: ComponentProps<'label'>) {
	return null
}
